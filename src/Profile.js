import React from "react";
import { View } from "react-native";

export default function Profile(props) {
    
    return (
        <View>
            <Text>C'est la page de profile</Text>
        </View>
    );
}
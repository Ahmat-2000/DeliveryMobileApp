import React from "react";
import { View } from "react-native";

const Home = (props) => {
    
    return (
        <View>
            <Text>C'est la page d'accueil</Text>
        </View>
    );
}
export default Home;
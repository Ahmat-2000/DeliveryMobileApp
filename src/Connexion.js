import React from "react";
import { View } from "react-native";

export default function Connexion(props) {
    
    return (
        <View>
            <Text>C'est la page de connextion</Text>
        </View>
    );
}